# meta/backlog.md — 模板演进候选储备

> 维护者 backlog:已识别但未立项的候选方向、评估结论与触发信号,供跨会话盘点接力。
> 不占 RFC 编号 —— RFC 留给定案的提案,本文件承接未定案的方向;立项时从条目「路径」起草 RFC。
> 状态语义:**候选**(识别未评估)/ **观望**(评估过,等触发)/ **待设计**(有硬设计张力未解)。
> 治理档案不点名具体下游项目(与 meta/README 表述规则一致)。
>
> **退出机制(防自体累积)**:① 落地即移除 —— 条目立项落地时从表内删除,权威记录在
> meta/README RFC 注册表与根 README 版本史,本文件不设「已关闭」表(第二份会漂移的声明);
> ② 失效即移除 —— 维护时点绑定盘点本身(不设周期义务),每次盘点对全表复审「触发信号是否
> 仍成立」,不再成立的条目直接删除,git 历史留痕。不变式:**表内只剩活的候选**。
>
> **最近全表复审:2026-09-15** —— 三候选触发信号均未触发,全部保留(本轮盘点即首轮复审)。

## 候选

| 候选 | 状态 | 评估结论与路径 | 触发信号 |
|---|---|---|---|
| design/assets 资产清单机器化(5.1 义务门禁) | 观望 | 两层变更前置,非「既存 MUST 补兜底」型:先在 structure.md 5.1 规范「资产清单」表格格式(沿 5.2 证据清单模式:逐文件 / 批量行,外部托管记链接与获取方式),design-template 加清单骨架;再新增检查(编号按届时序)双向校验 —— 资产目录 ↔ 顶层编号设计稿对应(孤儿目录)、清单 ↔ 实际文件(幽灵资产 / 清单漂移),表格解析可复用检查 8 提取逻辑。病灶:assets/ 当前完全无检查,5.1「未被引用的资产应删除」纯靠人工 | design/ 真实使用(assets/ 开始积累资产);在那之前人工执行 5.1 两条 bullet 够用,为不存在的目录立法属流程税 |
| spec:tasks 缺配对检查(检查 4 补 count<2 方向) | 待设计 | 恒 1:1 是 decisions 第 3 节明文机制,检查 4 只查超配(count>2)不查缺配;但立项流程 spec 与 tasks 逐份确认产出,「同编号仅 spec 无 tasks」是合法中间态 —— 严格报错打断流程。前置:豁免口径(如按注册表状态区分「未完成中间态」与「违规缺失」)未设计。2026-09-17:触发信号之二兑现(检查 12 落地使注册表状态机器可判),立项决策待维护者 | 出现实际漏写 tasks 的项目事故;或注册表状态语义稳定到可机器判定中间态(已兑现) |
