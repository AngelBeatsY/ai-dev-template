# 项目状态

> 活文档:每项目一份,初始化时开始填写。
> 定位:**跨会话连续性的锚点** —— 任何 AI 会话开始时先读本文件恢复上下文;会话结束或中断前更新本文件。规则见 [conventions/workflow.md](conventions/workflow.md) 第 6 节。

## 当前状态

TODO(template):一段话概括项目当前所处阶段与正在进行的最重要的事。

## 进行中

| 事项 | 关联编号 | 进度 | 负责人 |
|---|---|---|---|
| TODO(template):(如 SPEC-0001 用户认证) | | <如 tasks T1-T3/T6 完成> | |

## 下一步

TODO(template):按优先级列出,每条可判定、可指向 spec/brief/ADR。

1. TODO(template):

## 已知问题

TODO(template):已知但暂不处理的问题,附接受原因或关联 spec;无则写「无」。

## 工作日志

> **只追加,不覆盖。** 每条三要素:完成(本次做了什么、关键决策与结论)、下次接(从哪里继续、前置动作)、关联(commit hash / spec 编号)。中断恢复时从最后一条往后读。
> 行形态(v2.7.1):条目 = `- YYYY-MM-DD(续):<三要素>`,同日多条加(续)标记;三要素有则必写——无接续事项时「下次接」写显式收口语(如「无(M3 已收口)」),不省略要素;关联列装 commit hash / 编号,不装解释。本节为会话交接面,无机器核对,形态以可扫读为度,不做更强约束。

- YYYY-MM-DD(示例行,首次使用时删除):完成 <…,含关键决策>;下次接 <…,前置动作明确到命令级>;关联 <commit / SPEC-NNNN>。
