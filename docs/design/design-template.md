<!--
设计文档模板。复制本文件到 docs/design/NNNN-<slug>.md 并填写(编号与来源 spec 同号同 slug,spec 为 Draft 即可挂靠)。
适用:验收标准依赖界面视觉细节的功能(详见 workflow.md 第 3.2 节)。
定位:轻量骨架,可选章节按设计重量增删。目录组织(多文件资产)见 structure.md 第 5.1 节。
HTML 注释为指导语,填写后删除。
-->

# 设计:<标题>

| 字段 | 值 |
|---|---|
| 编号 | <与 spec 同前缀,如 0001-settings-ui> |
| 来源 spec | SPEC-NNNN |
| 状态 | Draft |
| 设计人 | <姓名> |
| 创建日期 | YYYY-MM-DD |

## 设计基准

<!-- 必填:本设计依据什么。spec 的相关 FR、现有设计系统、可复用组件。 -->

- 依据 FR:<SPEC-NNNN 的相关 FR 编号>
- 设计系统 / 组件库:<链接或「自定」>
- 可复用范式:<既有页面/组件,附链接>

## 整体布局

<!-- 必填:页面/组件的整体结构与分区。优先 Mermaid 或 ASCII 示意图(附文字摘要)。 -->

## 组件与交互

<!-- 必填:涉及哪些组件、它们的交互行为。
交互状态写全:默认 / hover / active(focus) / disabled;有过渡动画的注明时长与缓动。
「与现有组件一致」必须指到具体组件名 / class 名或设计系统文档章节,不允许无据引用。
轻量设计可合并相近状态描述,可交互元素的 hover 与 disabled 不可省略。 -->

| 组件 | 作用 | 交互 / 状态 |
|---|---|---|
| TODO(template): | | |

## 响应式

<!-- 涉及多端时必写;纯桌面/后端无 UI 删除本节。 -->

| 断点 | 布局变化 |
|---|---|
| 桌面 > 1024px | |
| 平板 ≤ 1024px | |
| 移动 ≤ 600px | |

## 视觉规格

<!-- FR 依赖视觉细节(颜色 / 间距 / 字号)时必填,其余可选。
标注纪律:颜色写 token 或色值,间距写 px,字号写 px + 字重;不写「美观」「协调」类不可判定描述。
引用既有样式 MUST 指到 token 名 / class 名 / 组件名。
确需设计系统之外的新值时标注为新提案,由实现任务落地,不在本文档维护第二份规范(structure.md 第 5.1 节「原型与生产分离」)。 -->

## 资产清单

<!-- 有独立文件资产(原型 html、效果图 png、tokens css 等)时必填;无则删除本节。
资产归档到 assets/<编号>/,每个资产在此列出并说明用途(见 structure.md 第 5.1 节)。 -->

| 文件 | 类型 | 用途 |
|---|---|---|
| assets/NNNN-xxx/ | | |

## 实现回标

<!-- 实现与设计稿不一致时,设计文档的更新义务,两级处理:
- 轻微(间距、色值等视觉微调,FR 不受影响):在「变更记录」记一行即可,不逐节标注。
- 实质(组件移除 / 替换、交互行为变化、影响 FR):不删原稿,在受影响小节加「> 实现说明:…」引用块
  (说明已移除 / 已废弃 / 未落地,附替代方案或 SPEC 编号),并在「变更记录」补行。
纯新增内容(实现补齐了设计未覆盖的状态)在「变更记录」记一行即可。
设计文档随实现演进,不是一次性交付。 -->

## 变更记录

<!-- 设计迭代在此记行:日期、改了什么、原因。定稿后由 spec「接口设计」节链接本文件。 -->

| 日期 | 改动 | 原因 |
|---|---|---|
