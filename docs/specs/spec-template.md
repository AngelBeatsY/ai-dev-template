<!--
spec 模板:功能规格。复制本文件到 docs/specs/NNNN-<slug>.spec.md 并填写。
验收标准与错误路径是评审重点,详见 docs/conventions/workflow.md 第 3.2 节。
HTML 注释为指导语,填写后删除。
-->

# Spec-NNNN:<标题>

| 字段 | 值 |
|---|---|
| 编号 | SPEC-NNNN |
| 状态 | Draft |
| 来源 | BRIEF-NNNN / RFC-NNNN / 无(自发;编号即 specs/ 内 max+1) |
| 负责人 | <姓名> |
| 创建日期 | YYYY-MM-DD |
| 更新日期 | YYYY-MM-DD |

<!-- 状态值 MUST 为裸枚举词(生命周期见 docs/README.md),不带日期与后缀——修订时点见「更新日期」,迁移历史见「变更记录」,取代互链记文件头状态区注记行。
「更新日期」= 本文件最后修订日(任何修改,含状态行、衔接行、账面追补),MUST 取 git log 实测,禁止凭记忆填写;注册表「更新日期」列同语义,修改本文件的会话同一 commit 刷新(谁改谁刷)。
状态与更新日期双面受 verify-docs 检查 12 校验(RFC-0019/0020)。 -->

## 概述

<!-- 一段话:做什么、给谁、达成什么。不超过 5 句。 -->

## 现状盘点

<!-- 必填(新功能/改行为类):逐条列出与本设计相关的现状。
每条 MUST 附证据(文件链接与行号、或调研/实测结论),不得转述他人结论或凭记忆断言。
「对设计的影响」列必写 —— 盘点的目的是推导方案,不是罗列见闻。
关联调研结论链接 docs/research/ 对应文件。 -->

| # | 现状(附证据) | 对本设计的影响 |
|---|---|---|
| 1 | | |

## 验收标准

<!-- 必填:编号 FR-1 起,Given-When-Then 式,每条独立可测、可判对错。
「功能正常」「体验良好」「性能可以」不算标准 —— 写可判定的条件与数值。 -->

- **FR-1**:Given <前置状态> When <操作/事件> Then <可观察的结果>
- **FR-2**:…

## 接口设计

<!-- 按适用保留小节,不适用的删除。 -->

### API / 函数签名

<!-- 端点、参数、返回值、错误码;与现有接口的风格一致。
对外错误路径 ≥ 3 条时,在此集中「错误契约」表:场景 / HTTP / 错误码 / 附加字段,
作为对外错误的唯一格式口径;「边界与错误路径」的 ER-N 引用表内错误码描述触发场景与系统行为,
不重复定义格式;消费方对接与测试计划以本表为准。 -->

### 数据结构

<!-- 新增或修改的类型、字段、约束。 -->

### CLI / 配置

<!-- 命令、参数、配置项及默认值。 -->

### UI / 交互

<!-- 涉及界面时必写:轻量场景直接内嵌交互要点;有独立设计稿时链接 docs/design/ 对应文档(实例化自 design-template.md,组织规则见 structure.md 第 5.1 节)。
验收标准依赖视觉细节时,设计稿定稿是 spec 转 Active 的前置(workflow.md 第 3.2 节)。 -->

## 边界与错误路径

<!-- 必填:编号 ER-1 起,显式列举。输入校验失败、依赖不可用、并发冲突、超时等。
方案依赖外部系统行为(第三方 API、回调、DNS)时,结论 MUST 来自实测或调研落档(docs/research/),
不把「文档说应该这样」当依据。禁止「异常情况酌情处理」。 -->

- **ER-1**:<场景> → <系统的行为与用户可见的错误>
- **ER-2**:…

## 兼容性影响

<!-- 必填:是否破坏向后兼容;破坏时给出迁移方式(并确认已有 Accepted 的 RFC)。写「无影响」也要显式声明。 -->

## 测试计划

<!-- 每条 FR 至少对应一个用例;ER 逐条覆盖。 -->

| 验收标准 | 测试方式 | 层级(单元/集成/E2E) |
|---|---|---|
| FR-1 | <用例描述> | <层级> |
| ER-1 | <用例描述> | <层级> |

## 风险与缓解

<!-- 推荐填写,无风险可删除本节。每条三要素:风险、后果、规避动作。
范本:「改密业务错误若返 401,前端会清 cookie 跳登录 —— 必须 400,测试显式断言 ≠ 401」。
只写带后果与规避的具体风险,不写空泛提醒。 -->

| # | 风险 | 后果 | 规避动作 |
|---|---|---|---|
| 1 | | | |

## 非功能要求

<!-- 可选:性能、安全、可观测性方面的量化要求。无则删除本节。 -->

## 开放问题

- [ ] <问题> → <结论>(YYYY-MM-DD)

## 变更记录

<!-- 状态迁移与正文修改(Active 后)均在此记行:日期、内容(迁移 / 修改)、原因。Draft 期正文自由修改不记行(RFC-0019)。 -->

| 日期 | 修改 | 原因 |
|---|---|---|
