<!--
tasks 模板:任务拆解。复制本文件到 docs/specs/NNNN-<slug>.tasks.md,与对应 spec 同前缀。
拆分原则见 docs/conventions/workflow.md 第 3.3 节。HTML 注释为指导语,填写后删除。
-->

# Tasks:SPEC-NNNN <标题>

| 字段 | 值 |
|---|---|
| 对应 Spec | SPEC-NNNN |
| 创建日期 | YYYY-MM-DD |

## 开工评审

<!-- 施工会话开工前填写(workflow.md 第 3.4 节 DoR 与开工评审)。全部通过写「通过」;有疑义逐条列出(问题、涉及条目、处置)。续作会话不重填。 -->

- DoR 核对:<通过 / 不满足项>
- 证据抽查:<抽查的声明点与核对结果(file:line)>
- 正确性与可实施性:<结论>
- 疑义清单:<无 / 逐条:问题、涉及条目、处置>

## 任务清单

<!-- 每个任务:≤ 半天、单一关注点、指到文件级、带验收点;顺序 = 依赖顺序。
T 为实现任务,TF 为固定收尾任务(不可删除)。
任务较多或跨层(如数据层 → 后端 → 前端)时按 Phase 分组(`### P1 <名称>` 小节):
组内顺序即依赖顺序;每组收口时系统必须可运行、测试与 lint 全绿(不留半破状态)。
拆分判据见 workflow.md 第 3.3 节:跨层按依赖拆、单 Phase 超过拆分线、外部卡点独立成期并写降级路径。
每个 Phase 声明「交付边界」:该组完成时可验收的边界(如「契约可用」「页面可达」),写在分组小节开头;组内任务验收点说明如何证明边界达成。 -->

- [ ] **T1**:<任务描述>
  - 涉及文件:<路径列表>
  - 验收点:<完成标志>
- [ ] **T2**:…
- [ ] **TF1**:更新相关文档(tech-stack.md / architecture.md / 受影响的 spec;登记的自建活文档如有)
- [ ] **TF2**:spec 状态收口四动作(同一 commit,见 workflow.md 3.6):文件头状态行 / 变更记录迁移行 / 更新日期 / 注册表行同步
- [ ] **TF3**:对照 SPEC-NNNN 的 FR/ER 逐条自检,在下方自检表记录结果

## Spec 自检表

<!-- TF3 的输出。PR 创建前每行必须有结论。 -->

| 验收标准 | 结论 | 备注 |
|---|---|---|
| FR-1 | <通过/偏差,见偏差记录 R?> | |
| ER-1 | | |

## 独立验收

<!-- 验收人填写(workflow.md 第 3.5 节):常规级及以上必做,小改删除本节。逐条声明点给证据下结论;测试与 lint 复跑、交付记录 hash 实测核对的结果记入复跑记录。 -->

| # | 声明点 | 结论(PASS/PARTIAL/FAIL) | 证据(file:line / 实测输出) | 备注 |
|---|---|---|---|---|
| 1 | | | | |

- 复跑记录:<测试与 lint 复跑结果;交付记录 hash 实测核对结果>
- 缺陷清单:<无 / MED / LOW / OBS 逐条,含触发路径>

## 交付记录

<!-- 每个任务/阶段完成后回填,与代码同一 commit(禁止代码先走、文档后补)。
交接要点面向下一个任务/下一次会话:完成了什么可直接依赖、还缺什么前置。
首个交付记录注明开工测试基线(全量 passed 数);后续条目与基线对照,低于基线 MUST 说明原因(合并冗余用例、移除失效用例等正当删减照实说明即可)。 -->

- **T1**(YYYY-MM-DD):
  - commit:<hash>
  - 测试与 lint:<通过数/结果>
  - 交接要点:<…>

## 偏差记录

<!-- 两级处理(workflow.md 第 3.4 节):轻微偏离(实现细节、补测试)记录即可;
实质偏离(契约、数据结构、边界、范围)必须先修订 spec 再继续。 -->

- **R1**:偏差:<实现与 spec 的差异>。级别:<轻微/实质>。理由:<为什么>。处置:<随记录 / 已修订 SPEC-NNNN>(YYYY-MM-DD)
